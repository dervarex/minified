/**
 * Shared utility APIs used across the project.
 * May be used by the Launcher, as there's no internal-only code in here, except for some parts of {@link com.dervarex.minified.utils.version}, which mostly can be used too, but be careful with those.
 *
 * <p>Use {@code http}, {@code json}, {@code sha}, and {@code exceptions} from application code.
 * The {@code network} package is internal support code and should not be used directly.</p>
 */
package com.dervarex.minified.utils;
